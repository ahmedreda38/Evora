services:
  postgres-db:
    image: postgres:18
    container_name: evora-db
    environment:
      POSTGRES_USER: db_admin
      POSTGRES_PASSWORD: evora123
      POSTGRES_DB: evoradb
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql
volumes:
  pgdata:
